/** @deprecated Prefer `@forgeax/editor-core` — kept for legacy `import from 'fbx-cook'` call sites. */
export { cookFbxMeta, type FbxCookResult, spawnAssetRefToScene, spawnAssetToScene } from '@forgeax/editor-core';
