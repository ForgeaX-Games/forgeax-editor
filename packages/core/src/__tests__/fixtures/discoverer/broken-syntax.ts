// w18 fixture: intentionally broken — syntax error
import { defineComponent } from '@forgeax/engine-ecs';

// Missing closing parenthesis — will cause an import failure
export const Bad = defineComponent('BadComp'
