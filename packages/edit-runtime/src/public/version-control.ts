// Stable Editor facade for the host-owned Version Control status-bar surface.
// Keep the contribution implementation private so Studio and other hosts do
// not depend on the edit-runtime directory layout.
export { versionControlStatusBarExtension } from '../version-control/contribution';
