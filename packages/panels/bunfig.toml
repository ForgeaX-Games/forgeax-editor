[test]
# Register happy-dom globals before any test runs so DOM/component tests
# (useNumberDraft, etc.) get a real document / Element / focus-event cascade.
# Mirrors packages/interface/bunfig.toml + __tests__/happydom.ts.
preload = ["./__tests__/happydom.ts"]
